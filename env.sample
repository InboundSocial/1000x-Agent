# Example local envs (used when not running via Doppler)
# Duplicate to .env and fill in values as needed

# Runtime
NODE_ENV=development
PORT=3000

# Secrets (example)
API_KEY=
DATABASE_URL=

# Amp CLI
AMP_API_KEY=
